import * as React from 'react';

interface IAppProps {
}

const FirstComponent: React.FunctionComponent<IAppProps> = (props) => {
  return <h2>Meu primeiro Componente</h2>;
};

export default FirstComponent;
